# Getting Started

```shell
composer install

# Run tests
./vendor/bin/phpunit tests

# Get product images
./bin/scrape

# Build frontend assets
npm install
npm run build

# Run server
php -S localhost:8888 -t public
```
